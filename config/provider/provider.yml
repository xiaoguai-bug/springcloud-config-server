eureka:
  client:
    service-url:
      defaultZone: http://eureka1:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

feign:
  hystrix:
    enabled: true
